[providers]
python = {}

[phases.setup]
aptPkgs = ["...", "gettext-base", "gettext"]

[start]
cmd = "python manage.py migrate --noinput && python seed_demo.py && python manage.py collectstatic --noinput && uvicorn config.asgi:application --host 0.0.0.0 --port $PORT"
